<form method="post" class="d-inline" action="{{ path('admin_soort_delete', {'id': soort.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soort.id) }}">
    <button class="btn btn-danger">Verwijderen</button>
</form>
