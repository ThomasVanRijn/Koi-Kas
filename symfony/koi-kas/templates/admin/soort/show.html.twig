{% extends 'base.html.twig' %}

{% block title %}Soort{% endblock %}

{% block main %}
    <div class="container">
        <h1>Soort</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soort.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ soort.naam }}</td>
            </tr>
            </tbody>
        </table>

        <a class="btn btn-success" href="{{ path('admin_karper_index') }}">Terug naar lijst</a>

        <a class="btn btn-warning" href="{{ path('admin_soort_edit', {'id': soort.id}) }}">Bewerken</a>

        {{ include('admin/soort/_delete_form.html.twig') }}
    </div>
{% endblock %}
