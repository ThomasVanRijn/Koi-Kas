{% extends 'base.html.twig' %}

{% block title %}Kweker index{% endblock %}

{% block body %}
    <h1>Kweker index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Naam</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for kweker in kwekers %}
            <tr>
                <td>{{ kweker.id }}</td>
                <td>{{ kweker.naam }}</td>
                <td>
                    <a href="{{ path('kweker_show', {'id': kweker.id}) }}">show</a>
                    <a href="{{ path('kweker_edit', {'id': kweker.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('kweker_new') }}">Create new</a>
{% endblock %}
