<form method="post" action="{{ path('blog_image_delete', {'id': blog_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_image.id) }}">
    <button class="btn">Delete</button>
</form>
